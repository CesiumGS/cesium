describe("Scene/ModelExperimental/LightingPipelineStage", function() {
  it("configures the lighting model in the shader", function() {
    fail();
  });

  it("adds the lighting shader function to the shader", function() {
    fail();
  });
});