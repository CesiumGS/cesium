describe("Scene/ModelExperimental/MaterialPipelineStage", function() {
  it ("adds material uniforms to shader", function() {
    fail();
  });

  it("adds metallic roughness uniforms to shader", function() {
    fail();
  });

  it("adds specular glossiness uniforms to shader", function() {
    fail();
  });

  it("configures alpha settings", function() {
    fail();
  });

  it("configures lighting model", function() {
    fail();
  });

  it("adds material processing functions to the shader", function() {
    fail();
  })
});