If subtree availability is many levels, break into 2 layers of subtrees(each can have differnt umber of levels)
maybe if not needed save off the relative boxes for subtree so that it can be constructed later.
