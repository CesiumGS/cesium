apiVersion: v1
kind: Service
metadata:
  name: {{.Values.application.name}}
  labels:
    run: cesium-standalone
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: cesium-standalone
  type: NodePort