import { Text } from "@stratakit/bricks";

export function ModelSettings() {
  return (
    <>
      <Text variant="headline-sm">Models</Text>
      <Text variant="body-md">
        Model selection is available in the chat header. Use the API
        Configuration dialog to add or update API keys.
      </Text>
    </>
  );
}
