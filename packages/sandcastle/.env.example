# Copy to .env.local and fill in to enable analytics during development.
# Leave unset to keep analytics disabled (the default).
VITE_AMPLITUDE_API_KEY=
# Label attached to every analytics event identifying the deployment that
# sent it, e.g. "main" or "ci-branch". Events report "local" when unset.
VITE_ANALYTICS_ENVIRONMENT=
