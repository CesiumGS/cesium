// Trivial placeholder export so build/test tooling has real content to bundle
// against until real files land in Phase 3d.
class Placeholder {
  constructor() {
    this.value = true;
  }
}

export default Placeholder;
