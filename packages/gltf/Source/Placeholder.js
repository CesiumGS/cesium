/**
 * Temporary placeholder module that keeps this package's directory tracked
 * and exercises barrel generation, declaration output, and package-scoped
 * tests before the glTF pipeline source files are moved here.
 *
 * @function
 * @internal
 */
function Placeholder() {}

export default Placeholder;
